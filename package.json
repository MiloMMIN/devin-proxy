{
  "name": "devin-proxy",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts serve",
    "dev": "bun run --watch src/index.ts serve",
    "login": "bun run src/index.ts login",
    "status": "bun run src/index.ts status",
    "models": "bun run src/index.ts models",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.4.2",
    "typescript": "^5.9.3"
  }
}
